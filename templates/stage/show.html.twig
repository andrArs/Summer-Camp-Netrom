{% extends 'base.html.twig' %}

{% block title %}HelloController!{% endblock %}

{% block body %}
    <style>
        body {
            background-color: #2e1a47;
            font-family: 'Poppins', sans-serif;
            text-align: center;
        }
        .example-wrapper {
            margin: 2em auto;
            max-width: 700px;
            background-color: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 0 15px rgba(111, 66, 193, 0.3);
        }
        .btn-container {
            display: flex;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
            margin-top: 1.5rem;
        }

        .btn-danger {
            background-color: #6f42c1;
            border-color: #6f42c1;
        }

        .btn-danger:hover {
            background-color: #5a35a2;
            border-color: #5a35a2;
        }

        .modal-content {
            border: 2px solid #6f42c1;
            border-radius: 12px;
        }

        .modal-header {
            background-color: #f3e6f5;
        }

        .image {
            width: 100%;
            height: 250px;
            border-radius: 15px;
            object-fit: cover;
            margin-bottom: 1.5rem;
            box-shadow: 0 4px 15px rgba(123, 47, 247, 0.6);
            border: 3px solid #a57eff;
        }

    </style>

    <div class="example-wrapper">
        <h1>Details about {{ stage.name }}!</h1>

        <h3>Genre: <strong>{{ stage.genre}}</strong></h3>


        <ul>
            {{ stage.description }}
             </ul>

        <img
            src="https://solotech.com/wp-content/uploads/2024/08/Veld-Night-1-7.jpg"
            alt="Festival Image"
            class="image">


        {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('edit_stage',  {'id': stage.id}) }}" class="btn btn-danger" >
            Edit Stage
        </a>

        {#            button trigger modal#}
        <div class="btn-container">
            <button type="button"
                    class="btn btn-danger"
                    data-bs-toggle="modal"
                    data-bs-target="#deleteModal{{ stage.id }}">
                Delete
            </button>

            {% endif %}

            <a href="{{ path('all_stages') }}" class="btn btn-outline-secondary">Back to list</a>
        </div>

        {#            modal#}
        <div class="modal fade"
             id="deleteModal{{ stage.id }}"
             tabindex="-1"
             aria-labelledby="deleteModalLabel{{ stage.id }}"
             aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">

                    <form action="{{ path('delete_stage', {'id': stage.id}) }}" method="post">

                        <div class="modal-header">
                            <h5 class="modal-title"
                                id="deleteModalLabel{{ stage.id }}">Are you sure you want to delete the this stage?</h5>

                            <button type="button"
                                    class="btn-close"
                                    data-bs-dismiss="modal"
                                    aria-label="Close"></button>
                        </div>

                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </div>
                    </form>

                </div>
            </div>
        </div>




    </div>
{% endblock %}
